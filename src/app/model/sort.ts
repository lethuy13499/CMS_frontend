export class Sort  {
    sortName: string;
    sort: boolean;
    keyword: string;
         constructor (sortName: string, sort: boolean, keyword: string) {
        this.sortName = sortName,
        this.sort = sort,
        this.keyword = keyword;
            }
   }
